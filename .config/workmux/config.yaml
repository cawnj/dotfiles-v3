nerdfont: true
agent: claude

# disable automatic tmux format modification
status_format: false

panes:
  - command: <agent>
  - split: horizontal
    focus: true

files:
  symlink:
    - docs/superpowers

sandbox:
  enabled: false
  agent_config_dir: ~/.claude-sandbox
  container:
    runtime: docker
